# Pipline Steps
1. we add the changed files to github repositry
2. we commit and then we push the application to github
3. then circleci will run a build that run the commands in the config.yaml file
4. then we test the application
5. we deploy the application
6. repeat the steps if needed
